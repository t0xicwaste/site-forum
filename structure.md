forum-project/
├── backend/                 # Бэкенд-приложение (FastAPI)
│   ├── app/
│   │   ├── api/             # Маршруты (Эндпоинты) API
│   │   │   ├── v1/
│   │   │   │   ├── auth.py  # Логин, регистрация, JWT
│   │   │   │   ├── users.py # Профили пользователей
│   │   │   │   └── posts.py # Темы, ветки, комментарии
│   │   │   └── router.py    # Главный роутер, объединяющий все эндпоинты
│   │   │
│   │   ├── core/            # Глобальные настройки проекта
│   │   │   ├── config.py    # Чтение переменных окружения (.env)
│   │   │   └── security.py  # Хеширование паролей, генерация JWT
│   │   │
│   │   ├── db/              # Настройка базы данных
│   │   │   ├── session.py   # Создание асинхронной сессии SQLAlchemy
│   │   │   └── base.py      # Базовый класс для моделей (для Alembic)
│   │   │
│   │   ├── models/          # SQLAlchemy модели (Схема БД в Python)
│   │   │   ├── user.py
│   │   │   └── post.py
│   │   │
│   │   ├── schemas/         # Pydantic схемы (Валидация входящих/исходящих данных)
│   │   │   ├── user.py
│   │   │   └── post.py
│   │   │
│   │   ├── services/        # Бизнес-логика (CRUD операции, работа с БД)
│   │   │   ├── user_service.py
│   │   │   └── post_service.py
│   │   │
│   │   └── main.py          # Точка входа в FastAPI приложение
│   │
│   ├── alembic/             # Каталог миграций базы данных (создается автоматически)
│   ├── alembic.ini          # Конфигурация Alembic
│   ├── Dockerfile           # Инструкция сборки бэкенд-контейнера
│   └── requirements.txt     # Зависимости (fastapi, sqlalchemy, asyncpg, pydantic и др.)
│
├── frontend/                # Фронтенд-приложение (Vue.js 3 + Vite)
│   ├── src/
│   │   ├── assets/          # Статические файлы (логотипы, глобальные стили)
│   │   ├── components/      # Переиспользуемые UI-компоненты (кнопки, карточки постов, формы)
│   │   ├── composables/     # Переиспользуемая логика (Vue composables)
│   │   ├── router/          # Настройка маршрутизации страниц (Vue Router)
│   │   │   └── index.js
│   │   ├── store/           # Управление состоянием (Pinia)
│   │   │   ├── auth.js      # Хранение токена и данных текущего юзера
│   │   │   └── posts.js     # Временное хранение списка тем/комментариев
│   │   ├── views/           # Полноценные страницы (Компоненты-экраны)
│   │   │   ├── LoginView.vue
│   │   │   ├── HomeView.vue # Список всех веток форума
│   │   │   └── PostView.vue # Конкретная тема с комментариями
│   │   ├── App.vue          # Главный корневой компонент
│   │   └── main.js          # Инициализация Vue приложения
│   │
│   ├── index.html           # Базовый HTML-шаблон
│   ├── tailwind.config.js   # Настройки стилей Tailwind
│   ├── vite.config.js       # Конфигурация сборщика Vite
│   ├── Dockerfile           # Инструкция сборки фронтенд-контейнера (Nginx/Node)
│   └── package.json         # Зависимости фронта (vue, pinia, axios, tailwindcss)
│
├── nginx/                   # Настройки обратного прокси для Production
│   └── default.conf         # Конфиг Nginx для маршрутизации трафика между фронтом и бэком
│
├── .env                     # Локальные переменные окружения (секреты, пароли к БД)
└── docker-compose.yml       # Скрипт для одновременного запуска всех сервисов в контейнерах